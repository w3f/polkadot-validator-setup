[Unit]
Description=Polkadot Node

[Service]
ExecStart=/usr/local/bin/polkadot \
         --name {{ project }}-sv-public-{{ groups['public'].index(inventory_hostname) }} \
         --chain={{ chain }} \
         {% if hostvars[inventory_hostname].telemetryUrl %}
         --telemetry-url {{ hostvars[inventory_hostname].telemetryUrl }}
         {% else %}
         --no-telemetry
         {% endif %}

Restart=always

[Install]
WantedBy=multi-user.target